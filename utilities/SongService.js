
  // type Song = { title: string, artist: number, partOne: string, partTwo: string }

      export default function changeSong () {
        // const randomSong: Song = this.getRandomSong(this.state.songsArr);
        console.log("test");
      }
    
      // getRandomSong(arr: Array<Song>): Song {
      //   const song: Song = arr[Math.floor(Math.random() * arr.length)];
    
      //   song.partOne = this.createNewLinesInsteadOfHyphens(song.partOne);
      //   song.partTwo = this.createNewLinesInsteadOfHyphens(song.partTwo);
      //   return song;
      // }
    
      // createNewLinesInsteadOfHyphens(lyricPart: string) {
      //   return lyricPart.replace(/-/g, '\n\n');
      // }
